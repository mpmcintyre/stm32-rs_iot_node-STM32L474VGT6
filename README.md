# Rust on STM32 IoT Node - Blinky

This repo is a basic starting point and testing kit for rust + Embassy on the STM32 IoT Node Disco kit (STM32L474VGT6)

## Blinky

Toggles builtin LED on and off every second while printing via GDB `Hello` and `World!`
